runtime: go
env: flexible # or flex

# default is enough for local testing
resources:
    cpu: 4

automatic_scaling:
   min_num_instances: 1
   max_num_instances: 2
# only one shows in admin server (:8000)

## defaults on gcloud are
# env: flexible
# threadsafe: true
# automatic_scaling:
#  min_num_instances: 2
#  max_num_instances: 20
# cpu_utilization:
#  target_utilization: 0.5
